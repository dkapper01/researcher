extends layout

block content

    h1 Delete firm: #{firm.firm_name}


    form(method='POST' action='')
        div.form-group
            input#id.form-control(type='hidden',name='id', required='true', value=firm._id )

        button.btn.btn-primary(type='submit') Delete